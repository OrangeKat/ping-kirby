# Backend
Dependencies:
- Maven
- jdk17

To launch the backend use the command:
```
> cd back
> mvn quarkus:dec
```

# Frontend
Dependencies:
- npm

To launch the front end use the command:
```
> cd front
> npm install
> npm run dev
```
